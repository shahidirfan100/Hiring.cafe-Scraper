{
    "title": "Hiring.Cafe Jobs Scraper",
    "description": "Extract job listings from Hiring.Cafe using the live Next.js SSR search payload.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Hiring.Cafe URL used to bootstrap the SSR search payload.",
            "editor": "textfield",
            "default": "https://hiring.cafe",
            "prefill": "https://hiring.cafe"
        },
        "keyword": {
            "title": "Keyword",
            "type": "string",
            "description": "Search keyword, for example: software engineer.",
            "editor": "textfield",
            "default": "software engineer",
            "prefill": "software engineer"
        },
        "location": {
            "title": "Location",
            "type": "string",
            "description": "Optional location filter. Exact location mapping currently supports United States.",
            "editor": "textfield",
            "default": "United States",
            "prefill": "United States"
        },
        "workplaceType": {
            "title": "Workplace Type",
            "type": "string",
            "description": "Filter by workplace type.",
            "editor": "select",
            "enum": ["Any", "Remote", "Hybrid", "Onsite"],
            "enumTitles": ["Any", "Remote", "Hybrid", "Onsite"],
            "default": "Any",
            "prefill": "Any"
        },
        "results_wanted": {
            "title": "Maximum jobs",
            "type": "integer",
            "description": "Maximum number of jobs to save to dataset.",
            "minimum": 1,
            "default": 20,
            "prefill": 20,
            "editor": "number"
        },
        "max_pages": {
            "title": "Maximum pages",
            "type": "integer",
            "description": "Safety cap for SSR pagination.",
            "minimum": 1,
            "default": 10,
            "prefill": 5,
            "editor": "number"
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Proxy settings for future fallback strategies if Hiring.Cafe changes again.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true,
                "apifyProxyGroups": ["RESIDENTIAL"]
            }
        }
    },
    "required": [],
    "additionalProperties": false
}
